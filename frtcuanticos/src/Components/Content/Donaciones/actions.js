import axios from 'axios';
import { paxios } from '../../../Utilities/axios';

export const tipocuenta = async () => {

    try {
        let result = //consuklta al backend
        return result;
    } catch (e) {
        throw e;
    }
}
